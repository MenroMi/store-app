import { styled } from '@mui/material';

export const CustomPopularTermsWrapper = styled('div')({
  flexDirection: 'column',
  justifyContent: 'space-between',
  maxWidth: '500px',
  width: '100%',
});
