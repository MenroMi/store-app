import { Box, styled } from '@mui/material';

export const CustomSearchOverlay = styled(Box)({
  position: 'fixed',
  top: '0',
  left: '0',
  zIndex: '9',
  width: '100%',
  height: '100%',
});
